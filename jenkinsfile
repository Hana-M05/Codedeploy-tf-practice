pipeline {
    agent any
    parameters {
        string(name: 'TARGET_VERSION', defaultValue: '2.0', description: 'Enter Version to Deploy')
    }
    stages {
        stage('Trigger Deployment') {
            steps {
                script {
                     withCredentials([
                    string(credentialsId: 'AWS_ACCESS_KEY_ID', variable: 'AWS_ACCESS_KEY_ID'),
                    string(credentialsId: 'AWS_SECRET_ACCESS_KEY', variable: 'AWS_SECRET_ACCESS_KEY'),
                    string(credentialsId: 'AWS_SESSION_TOKEN', variable: 'AWS_SESSION_TOKEN')
                ]) {
                    // Save the version to AWS Parameter Store
                   sh "aws ssm put-parameter --name '/app/version' --value '${params.TARGET_VERSION}' --type 'String' --overwrite --region us-west-2"
                    // Removed the ${ } from the repository and commitId
                   sh """
                    aws deploy create-deployment \
                        --application-name Htest-web-app \
                        --deployment-group-name Htest-deployment-group \
                        --github-location repository=Hana-M05/Codedeploy-tf-practice,commitId=8b241eed70c5eb09a8a319ea1cb860ba50af3a04 \
                        --region us-west-2
                    """
                  }
                }
            }
        }
    }
}